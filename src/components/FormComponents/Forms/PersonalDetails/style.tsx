import { SxProps } from "@mui/material"

const styles: {
  [key: string]: SxProps
} = {
  personalDetailsWrapper: {
    display: "flex",
    flexDirection: "column",
    padding: "100px 0",
  },
}

export default styles
