import { SxProps } from "@mui/material"

const styles: {
  [key: string]: SxProps
} = {
  progressBarWrapper: {
    // display: "flex",
  },
  progressBarStyle: {
    height: "10px",
    margin: "0 8px",
    borderRadius: "5px",
  },
}

export default styles
